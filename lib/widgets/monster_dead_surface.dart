import 'package:flutter/material.dart';

class MonsterDeadSurface extends StatefulWidget {
  const MonsterDeadSurface({super.key});

  @override
  State<MonsterDeadSurface> createState() => _MonsterDeadSurfaceState();
}

class _MonsterDeadSurfaceState extends State<MonsterDeadSurface> {
  @override
  Widget build(BuildContext context) {
    return Stack(children: [

      ],
    );
  }
}
